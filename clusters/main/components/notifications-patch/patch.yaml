apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: not-used
  namespace: flux-system
spec:
  providerRef:
    name: discord
  eventSeverity: info
  eventSources:
    - kind: HelmRelease
      name: "*"
      namespace: "${NAMESPACE}"