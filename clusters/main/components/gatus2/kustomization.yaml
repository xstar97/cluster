apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
configMapGenerator:
  - name: gatus-config
    files:
      - configmap.yaml=./configmap.yaml
generatorOptions:
  disableNameSuffixHash: true
