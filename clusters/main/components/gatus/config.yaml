---
endpoints:
  - name: "${NAME}"
    enabled: "${ENABLED:-true}"
    url: "${URL:-${PROT}://${SVC}.${NAMESPACE}.svc.cluster.local:${PORT}}"
    group: "${GROUP}"
    interval: 1m    
    ui:
      hide-url: true
      hide-hostname: true
    client:
      insecure: true
      timeout: 30s
    conditions:
      - "[CONNECTED] == true"
    alerts:
      - type: discord
        description: "healthcheck failed"
        send-on-resolved: true
