apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: open-archiver
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/tools/open-archiver/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
