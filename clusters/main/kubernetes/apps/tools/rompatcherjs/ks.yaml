apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rompatcherjs
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/tools/rompatcherjs/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

