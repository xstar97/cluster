apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: discount-bandit
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/tools/discount-bandit/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

